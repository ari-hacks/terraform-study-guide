#The Terraform language has two different syntaxes for string literals. The most common is to delimit the string 
#with quote characters ("), like "hello". 
#In quoted strings, the backslash character serves
#as an escape sequence, with the following characters selecting the escape behavior: